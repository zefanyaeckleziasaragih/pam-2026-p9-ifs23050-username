name: pam_2026_p9_ifs23050_username
description: "UsernameAI - Generate username kreatif dengan AI"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  provider: ^6.0.5
  intl: ^0.19.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  launcher_name: ^1.0.2

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"

launcher_name:
  default: "UsernameAI"
  id: "UsernameAI"

flutter:
  uses-material-design: true
  assets:
    - assets/images/
